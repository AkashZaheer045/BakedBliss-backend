/**
 * Auth Module
 * Handles authentication and authorization
 */

const routes = require('./routes/authRoutes');

module.exports = {
    routes,
    // Add other module exports here (services, validators, etc.)
};
