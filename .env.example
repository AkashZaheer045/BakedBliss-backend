# Server Configuration
PORT=3000
NODE_ENV=development

# Database Configuration (MySQL/MariaDB)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=baked_bliss_dev
DB_LOGGING=false
DB_SSL=false

# JWT Secret (change this to a secure random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Firebase (Optional - only if you still need Firebase for other services)
# FIREBASE_ADMIN_SDK_PATH=./path/to/firebase-service-account.json
# Note: Firebase is no longer required for the database
